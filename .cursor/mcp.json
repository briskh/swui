{
  "mcpServers": {
    "sw": {
      "command": "sw",
      "args": [
        "mcp"
      ],
      "env": {
        "SW_MCP_URL": "https://agent.swqt.net/mcp",
        "SW_MCP_TOKEN": "${SW_MCP_TOKEN}"
      },
      "x-sw-managed": true
    }
  }
}
