---
description: SWS Agent First Hop — host-loaded surfaces and required first actions
alwaysApply: true
---

# SWS First Hop (project)

### Host-loaded surfaces

Cursor has loaded this always-on rule and project `AGENTS.md`, including the managed block between `<!-- SWS:agent-first-hop-index:begin -->` / `<!-- SWS:agent-first-hop-index:end -->`.
**MUST NOT** edit that managed block (humans or agents); refresh only via `sw repair --dimension host`.

### Canonical first-hop pointer

Read `.agents/skills/sw/SKILL.md` completely, then follow the global order only from `sws://reference/agent-first-hop-index.md`. This host adapter does not redefine that sequence or the routing Receipt.
Use `projectCompanionAnchor` / `sectionMap` only for the selected task-class slice; never preload the full companion.

Refresh managed host surfaces with `sw init --dir .` or `sw repair --dimension host --dir . --fix`.

Do not broadly discover MCP resources or guess tool/skill names before these actions.
