---
name: sw
description: Use the SW client (`sw mcp`) for team-owned MCP capabilities.
---


<!-- SWS:agent-first-hop-bridge:begin -->
## Project Agent Entry (Codex)

### Host-loaded surfaces

Codex has loaded project `AGENTS.md`; this selected `.agents/skills/sw` entry is the host-local SW bridge.

### Canonical first-hop pointer

Read `.agents/skills/sw/SKILL.md` completely, then follow the global order only from `sws://reference/agent-first-hop-index.md`. This bridge does not redefine that sequence or the routing Receipt.
The project companion map is `.cursor/skills/project-swqt-companion/SKILL.md`; read only the selected task-class slice, never the full companion by default.

The managed index remains between `<!-- SWS:agent-first-hop-index:begin -->` / `<!-- SWS:agent-first-hop-index:end -->` in project `AGENTS.md`.
**MUST NOT** edit that managed block (humans or agents); refresh only via repair.

Refresh managed host surfaces with `sw init --dir .` or `sw repair --dimension host --dir . --fix`.

<!-- SWS:agent-first-hop-bridge:end -->

# SW MCP Skill
